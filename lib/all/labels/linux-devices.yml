- name: Linux devices
  description: All Linux hosts
  query: SELECT 1 FROM os_version WHERE platform = 'ubuntu' OR platform = 'debian' OR platform = 'rhel' OR platform = 'centos' OR platform = 'fedora' OR platform LIKE 'opensuse%';
  label_membership_type: dynamic
